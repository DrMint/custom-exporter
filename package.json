{
  "name": "node-zpool",
  "private": true,
  "scripts": {
    "start": "bun run src/app.ts",
    "format": "prettier --write --list-different .",
    "tsc": "tsc",
    "precommit": "bun run tsc && bun run format",
    "upgrade": "ncu"
  },
  "dependencies": {
    "mqtt": "^5.15.2",
    "systeminformation": "^5.33.10"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "npm-check-updates": "^23.1.0",
    "prettier": "^3.9.6",
    "typescript": "^7"
  }
}
